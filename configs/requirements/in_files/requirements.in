# Core runtime dependencies for Deep Symbolic Optimization
click
colorama
json5
deap
dill
numba

# Allow any NumPy bugfix in the 1.x line (TensorFlow 2.19 currently requires <2.0)
numpy>=1.26,<2.0
pandas
pathos
prettytable
pyyaml
scikit-learn
seaborn
sympy
tqdm

# Use latest stable TF2 while preventing an unexpected major jump (adjust upper bound during future upgrades)
tensorflow>=2.19,<2.20

# For control tasks and RL
gymnasium[box2d,classic-control]>=1.0.0a1
stable-baselines3>=2.0
control

# The following package is Linux-only
# nvidia-cufile-cu12==1.11.1.6 ; platform_system != "Windows"

# Docs
mkdocs>=1.5
mkdocs-material[imaging]>=9.2
mkdocs-awesome-pages-plugin>=2.9
pymdown-extensions>=10
mkdocstrings[python]>=0.23
mkdocs-markdownextradata-plugin>=0.2.5

# Testing
pytest>=8
pytest-cov>=4

# Lint/format
black>=23
isort>=5.13
flake8>=7

# Type checking
mypy>=1.10

# Dependency tooling
pip-tools>=7
uv>=0.1

# NOTE:
# After modifying this file run:
#   uv pip compile requirements.in -o requirements.txt
# Keep the lock file in sync to avoid diverging TensorFlow versions.